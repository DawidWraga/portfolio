import { ToastContainer as Default } from 'react-toastify';

export interface ToastContainerProps {}

export function ToastContainer(props: ToastContainerProps) {
	const {} = props;

	return (
		<>
			<Default />
		</>
	);
}
